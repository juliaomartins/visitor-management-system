# The backend's LAN address. NOT localhost — on a phone, localhost is the phone.
# Change this when the server's IP changes, or every paired device breaks.
EXPO_PUBLIC_API_URL=http://192.168.1.50:8000
