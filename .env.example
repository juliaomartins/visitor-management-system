# Origin of the Django backend. The dashboard proxies /api and /media to it,
# so the browser only ever talks to the Next.js origin.
# At the event this is the server's static LAN address.
VMS_BACKEND_ORIGIN=http://localhost:8000
