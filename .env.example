# Where the backend lives on the LAN. The browser needs this at runtime for the
# WebSocket, so it must be NEXT_PUBLIC_. The same value drives the /api rewrite.
# NOT localhost: the screen is a different machine from the server.
NEXT_PUBLIC_VMS_BACKEND_ORIGIN=http://192.168.1.50:8000
